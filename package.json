{
  "name": "get-set-fetch-extension-root",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "bootstrap": "lerna bootstrap --ci",
    "clean": "lerna exec -- rimraf dist",
    "build": "npm run clean && lerna run build",
    "build:dist": "lerna run build:dist",
    "test": "lerna run test"
  },
  "keywords": [
    "scraper"
  ],
  "author": "Andrei Sabau",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/get-set-fetch/extension/issues"
  },
  "homepage": "https://github.com/get-set-fetch/extension#readme",
  "devDependencies": {
    "lerna": "^3.8.0",
    "rimraf": "^2.6.2"
  }
}
